import 'package:flutter/material.dart';

class InListTechnologyDivider extends StatelessWidget {
  const InListTechnologyDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 18);
  }
}