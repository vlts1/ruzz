import 'package:flutter/material.dart';

class AndroidGalleryIcon extends StatelessWidget {
  const AndroidGalleryIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.image_outlined);
  }
}
