import 'package:flutter/material.dart';

class AndroidWriteCommentIcon extends StatelessWidget {
  const AndroidWriteCommentIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.mode_edit_outline_outlined);
  }
}
