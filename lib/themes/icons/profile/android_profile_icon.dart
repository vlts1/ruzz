import 'package:flutter/material.dart';

class AndroidProfileIcon extends StatelessWidget {
  const AndroidProfileIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.person_outline_outlined);
  }
}
