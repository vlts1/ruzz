import 'package:flutter/material.dart';

class AndroidArrowIcon extends StatelessWidget {
  const AndroidArrowIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.arrow_right_rounded);
  }
}
