import 'package:flutter/material.dart';

class AndroidReplyIcon extends StatelessWidget {
  const AndroidReplyIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.reply_rounded, size: 18);
  }
}
