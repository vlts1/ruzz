import 'package:flutter/material.dart';

class AndroidSearchIcon extends StatelessWidget {
  const AndroidSearchIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.search);
  }
}
