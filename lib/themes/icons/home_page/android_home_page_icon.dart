import 'package:flutter/material.dart';

class AndroidHomePageIcon extends StatelessWidget {
  const AndroidHomePageIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.house_outlined);
  }
}
