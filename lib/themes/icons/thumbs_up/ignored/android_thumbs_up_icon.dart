import 'package:flutter/material.dart';

class AndroidThumbsUpIcon extends StatelessWidget {
  const AndroidThumbsUpIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.thumb_up_alt_outlined);
  }
}
