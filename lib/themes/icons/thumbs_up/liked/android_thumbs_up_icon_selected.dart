import 'package:flutter/material.dart';

class AndroidThumbsUpIconSelected extends StatelessWidget {
  const AndroidThumbsUpIconSelected({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.thumb_up_alt,
      color: Color.fromARGB(255, 255, 0, 0),
    );
  }
}
