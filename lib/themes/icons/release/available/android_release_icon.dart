import 'package:flutter/material.dart';

class AndroidReleaseIcon extends StatelessWidget {
  const AndroidReleaseIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.article_outlined, size: 20);
  }
}
