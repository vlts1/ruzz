import 'package:flutter/cupertino.dart';

class IosSummaryIcon extends StatelessWidget {
  IosSummaryIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(
      CupertinoIcons.quote_bubble,
      size: 20,
    );
  }
}
