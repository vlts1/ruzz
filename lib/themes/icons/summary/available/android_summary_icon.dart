import 'package:flutter/material.dart';

class AndroidSummaryIcon extends StatelessWidget {
  AndroidSummaryIcon({super.key});
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.format_quote_sharp,
      size: 20,
    );
  }
}
