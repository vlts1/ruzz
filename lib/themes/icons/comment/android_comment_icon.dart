import 'package:flutter/material.dart';

class AndroidCommentIcon extends StatelessWidget {
  const AndroidCommentIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.chat_bubble_outline_rounded, size: 20);
  }
}
