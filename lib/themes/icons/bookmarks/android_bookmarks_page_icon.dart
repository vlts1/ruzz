import 'package:flutter/material.dart';

class AndroidBookmarksPageIcon extends StatelessWidget {
  const AndroidBookmarksPageIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.bookmark_added_outlined, size: 24);
  }
}
