import 'package:flutter/material.dart';

class AndroidMyTechnologiesIcon extends StatelessWidget {
  const AndroidMyTechnologiesIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.list_alt, size: 24);
  }
}
