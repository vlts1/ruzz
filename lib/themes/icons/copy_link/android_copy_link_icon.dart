import 'package:flutter/material.dart';

class AndroidCopyLinkIcon extends StatelessWidget {
  const AndroidCopyLinkIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.copy, size: 21);
  }
}
