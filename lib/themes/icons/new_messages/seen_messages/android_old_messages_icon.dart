import 'package:flutter/material.dart';

class AndroidOldMessagesIcon extends StatelessWidget {
  AndroidOldMessagesIcon({super.key});
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.mail_outlined,
      size: 24,
    );
  }
}
