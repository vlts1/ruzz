import 'package:flutter/material.dart';

class AndroidUnseenMessagesIcon extends StatelessWidget {
  const AndroidUnseenMessagesIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.mark_email_unread_outlined, size: 24);
  }
}
